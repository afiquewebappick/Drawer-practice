# To use AsyncStorage in React Native

npm install @react-native-async-storage/async-storage

# Implement Deep Linking in React Native

In app.json, name "scheme" under "expo" to your desired scheme name.

To use deep linking in your app, you can use the Linking module from expo.